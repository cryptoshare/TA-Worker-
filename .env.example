# ---- Core ----
SYMBOL=HYPEUSDT              # e.g., BTCUSDT, HYPEUSDT
TF_LIST=5m,15m,1h,1d         # comma-separated TFs to compute snapshot
LOOKBACK=300                 # candles to fetch per TF
BYBIT_CATEGORY=linear          # spot | linear | inverse

# ---- Optional (file snapshot) ----
WRITE_SNAPSHOT_JSON=true

# ---- Optional Supabase (not required) ----
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
